apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleetflow-frontend-deployment
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: fleetflow-frontend
  template:
    metadata:
      labels:
        app: fleetflow-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-login-secret
      containers:
        - name: fleetflow-frontend
          image: ghcr.io/fleetflow-zarodolgozat/fleetflow-frontend:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: fleetflow-frontend-cluster-ip-service
spec:
  selector:
    app: fleetflow-frontend
  type: ClusterIP
  ports:
    - protocol: TCP
      targetPort: 80
      port: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fleetflow-frontend-certificate
spec:
  secretName: fleetflow-frontend-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - fleetflow.jcloud.jedlik.cloud
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fleetflow-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - fleetflow.jcloud.jedlik.cloud
      secretName: fleetflow-frontend-certificate
  rules:
    - host: fleetflow.jcloud.jedlik.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fleetflow-frontend-cluster-ip-service
                port:
                  number: 80
